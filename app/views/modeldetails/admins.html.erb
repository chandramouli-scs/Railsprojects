<h1>details of admins</h1>


<h4>total no of Admin/s - <%= Admin.all.count %></h4>
<% @admins.each do |admin| %>
	<%= admin.email %><br>
	<%= admin.created_at %><br>
<% end %>

	<div >
	<%= link_to "export", details_admins_path(format: "csv"), class: 'btn btn-primary' %>
		
	</div>